import React from 'react';
import {View, Text} from 'react-native';

interface GraphScreenProps {}

const GraphScreen = ({}: GraphScreenProps) => {
  return (
    <View>
      <Text>GraphScreen</Text>
    </View>
  );
};

export default GraphScreen;

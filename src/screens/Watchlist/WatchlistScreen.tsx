import React from 'react';
import {View, Text} from 'react-native';

interface WatchlistScreenProps {}

const WatchlistScreen = ({}: WatchlistScreenProps) => {
  return (
    <View>
      <Text>WatchlistScreen</Text>
    </View>
  );
};

export default WatchlistScreen;
